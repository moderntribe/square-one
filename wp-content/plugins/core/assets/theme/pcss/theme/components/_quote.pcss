/* -----------------------------------------------------------------------------
 *
 * Component: Quote
 *
 * ----------------------------------------------------------------------------- */

.c-quote {
	text-align: center;
}

.c-quote__text {
	font-size: 38px;
	font-weight: var(--font-weight-bold);
	line-height: 1.3;
}

.c-quote__cite {
	display: block;
	font-size: 16px;
	margin-top: var(--spacer);
}
