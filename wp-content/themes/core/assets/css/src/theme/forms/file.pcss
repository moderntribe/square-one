/* -----------------------------------------------------------------------------
 *
 * Forms: File
 *
 * ----------------------------------------------------------------------------- */

input[type="file"] {
	@mixin form-control;

	font-size: var(--form-font-size);
	height: auto;
	padding: var(--form-control-file-padding);
	line-height: 1;
}
