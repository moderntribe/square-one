/* -----------------------------------------------------------------------------
 *
 * Typography: Themes
 *
 * ----------------------------------------------------------------------------- */

/* -------------------------------------------------------------------------
 *
 * Theme: Light
 *
 * Notes:
 * + This can be used anywhere or can be paired up with .t-sink
 * + Also see the other base partials for use of this class as a parent
 *   selector (media, typography, etc.)
 *
 * Example:
 * <div class="t-theme--light">
 *   ...
 *   <div class="t-sink">
 *     ...
 *   </div>
 * </div>
 *
 * Example:
 * <div class="t-sink t-theme--light">
 *   ...
 * </div>
 *
 * ------------------------------------------------------------------------- */

.t-text--light {
	color: var(--color-white);

	* {
		color: var(--color-white);
	}
}

.t-theme--light {
	--color-bgd-content: var(--color-white);
	--color-text: var(--color-black);
}

/* -------------------------------------------------------------------------
 *
 * Theme: Dark
 *
 * Notes:
 * + Used by default in general, so not much probably needed here
 * + This can be used anywhere or can be paired up with .t-sink
 * + Also see the other base partials for use of this class as a parent
 *   selector (media, typography, etc.)
 *
 * Example:
 * <div class="t-theme--dark">
 *   ...
 *   <div class="t-sink">
 *     ...
 *   </div>
 * </div>
 *
 * Example:
 * <div class="t-sink t-theme--dark">
 *   ...
 * </div>
 *
 * ------------------------------------------------------------------------- */

.t-text--dark {

}

.t-theme--dark {
	--color-bgd-content: var(--color-black);
	--color-text: var(--color-white);
}
