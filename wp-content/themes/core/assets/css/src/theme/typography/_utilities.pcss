/* -----------------------------------------------------------------------------
 *
 * Utility Classes: Typography
 *
 * ----------------------------------------------------------------------------- */

/* -------------------------------------------------------------------------
 * Display X-Large
 * ------------------------------------------------------------------------- */

.t-display-x-large {
	@mixin t-display-x-large;
}

/* -------------------------------------------------------------------------
 * Display Large
 * ------------------------------------------------------------------------- */

.t-display-large {
	@mixin t-display-large;
}

/* -------------------------------------------------------------------------
 * Display
 * ------------------------------------------------------------------------- */

.t-display {
	@mixin t-display;
}

/* -------------------------------------------------------------------------
 * Display Small
 * ------------------------------------------------------------------------- */

.t-display-small {
	@mixin t-display-small;
}

/* -------------------------------------------------------------------------
 * Display X-Small
 * ------------------------------------------------------------------------- */

.t-display-x-small {
	@mixin t-display-x-small;
}

/* -------------------------------------------------------------------------
 * Display XX-Small
 * ------------------------------------------------------------------------- */

.t-display-xx-small {
	@mixin t-display-xx-small;
}

/* -------------------------------------------------------------------------
 * Body Large
 * ------------------------------------------------------------------------- */

.t-body-large {
	@mixin t-body-large;
}

/* -------------------------------------------------------------------------
 * Body
 * ------------------------------------------------------------------------- */

.t-body {
	@mixin t-body;
}

/* -------------------------------------------------------------------------
 * Body Small
 * ------------------------------------------------------------------------- */

.t-body-small {
	@mixin t-body-small;
}

/* -------------------------------------------------------------------------
 * Overline
 * ------------------------------------------------------------------------- */

.t-overline {
	@mixin t-overline;
}

/* -------------------------------------------------------------------------
 * Caption
 * ------------------------------------------------------------------------- */

.t-caption {
	@mixin t-caption;
}

/* -------------------------------------------------------------------------
 *
 * Theme: Light
 *
 * Notes:
 * + This can be used anywhere or can be paired up with .t-content
 * + Also see the other base partials for use of this class as a parent
 *   selector (media, typography, etc.)
 *
 * Example:
 * <div class="t-theme--light">
 *   ...
 *   <div class="t-sink">
 *     ...
 *   </div>
 * </div>
 *
 * Example:
 * <div class="t-content t-theme--light">
 *   ...
 * </div>
 *
 * ------------------------------------------------------------------------- */

.t-theme--light {
	color: var(--color-white);

	* {
		color: var(--color-white);
	}
}

/* -------------------------------------------------------------------------
 *
 * Theme: Dark
 *
 * Notes:
 * + Used by default in general, so not much probably needed here
 * + This can be used anywhere or can be paired up with .t-content
 * + Also see the other base partials for use of this class as a parent
 *   selector (media, typography, etc.)
 *
 * Example:
 * <div class="t-theme--dark">
 *   ...
 *   <div class="t-content">
 *     ...
 *   </div>
 * </div>
 *
 * Example:
 * <div class="t-content t-theme--dark">
 *   ...
 * </div>
 *
 * ------------------------------------------------------------------------- */

.t-theme--dark {

}
