{# Block: WYSIWYG #}

<div class="s-content">
	<div data-depth="0"
		data-name="columns"
		data-livetext
	>

		{% for col in columns %}
			<div class="s-sink t-sink"
					 data-depth="1"
					 data-index="{{ loop.index0 }}"
					 data-name="column_content"
					 data-autop="true"
					 data-livetext="true">
				{{ col }}
			</div>
		{% endfor %}

	</div>
</div>
