/*
 * Visual Editor CSS
 *
 * DO NOT EDIT THIS CSS FILE DIRECTLY. IT IS GENERATED BY GRUNT.
 * -------------------------------------------------------------
 * This file is just a clearing-house, see the scss directory
 * and edit the source files found at: "admin/editor-style.scss"
 *
 */

/* Reset */
@import "../base/reset.pcss";

/* Core */
@import "../utilities/all.pcss";
@import "../base/fonts.pcss";

/* -----------------------------------------------------------------------------
 *
 * Body & Globals
 *
 * ----------------------------------------------------------------------------- */

html .mceContentBody {
    padding: 15px !important;
}

body {
    color: var(--text);
    background-color: var(--white);
    font-weight: var(--font-weight-regular);
    font-size: 100%;
    line-height: var(--line-height-base);
    font-family: var(--font-family-base);
    -webkit-font-smoothing: antialiased;
    -webkit-hyphens: auto;
    -moz-hyphens: auto;
    -ms-hyphens: auto;
    hyphens: auto;
    text-rendering: optimizeLegibility;
    vertical-align: baseline;

    /* Typography */
    @import "../base/typography/default.pcss";
    @import "../base/typography/anchors.pcss";
    @import "../base/typography/headings.pcss";
    @import "../base/typography/lists.pcss";
    @import "../base/typography/quotes.pcss";
    @import "../base/typography/tables.pcss";

    /* Media */
    @import "../base/media/images.pcss";
    @import "../base/media/embeds.pcss";

    /* Buttons */
    @import "../base/buttons/default.pcss";

    /* Forms */
    @import "../base/forms/default.pcss";
    @import "../base/forms/labels.pcss";
    @import "../base/forms/fieldsets.pcss";
    @import "../base/forms/accessibility.pcss";
    @import "../base/forms/validation/default.pcss";
    @import "../base/forms/fields/radio-checkbox.pcss";
    @import "../base/forms/fields/select.pcss";
    @import "../base/forms/fields/file.pcss";
    @import "../base/forms/fields/textarea.pcss";
    @import "../base/forms/fields/submit.pcss";

}

/* -----------------------------------------------------------------------------
 *
 * Media
 *
 * ----------------------------------------------------------------------------- */

img,
iframe,
object,
embed,
video {
    border: 0;
    line-height: 0;
    width: auto;
    height: auto;
    margin-top: var(--spacer);
    margin-bottom: var(--spacer);
    display: block;
}

img {
    &[class*="align"],
    &[class*="wp-image-"],
    &[class*="attachment-"] {
        height: auto;
        display: block;
        max-width: 100%;
    }
    &.alignleft,
    &.alignright {
        margin-top: var(--spacer);
        margin-bottom: var(--spacer);
    }
}

.wp-caption {
    margin: var(--spacer) auto;
    width: auto !important;
    img {
        margin-bottom: 0;
        margin-top: 0;
    }
}

.wp-caption-text,
.entry-caption,
.wp-caption-dd {
    margin: 0 auto;
    padding: 16px 0;
    line-height: 1.333;
    font-size: 16px;
    text-align: left;
}

.wp-caption-dt {
    margin: 0;
}

.aligncenter {
    margin-left: auto;
    margin-right: auto;
    display: block;
}

.alignleft {
    float: left;
    &.wp-caption {
        margin-top: var(--spacer);
        margin-bottom: var(--spacer);
    }
}
.alignright {
    float: right;
    &img,
    &.wp-caption {
        margin-top: var(--spacer);
        margin-bottom: var(--spacer);
    }
}

.alignleft,
.alignright {
    max-width: 35% !important;
    &img,
    &.wp-caption {
        margin-bottom: var(--spacer);
    }
}