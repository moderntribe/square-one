/* -----------------------------------------------------------------------------
 *
 * Component: Button
 *
 * ----------------------------------------------------------------------------- */

a[class*="c-btn"],
button[class*="c-btn"] {

	.t-content & {
		margin-top: var(--spacer-1);
		margin-bottom: var(--spacer-1);
	}
}
