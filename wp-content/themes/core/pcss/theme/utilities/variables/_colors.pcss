:root {
	/* -----------------------------------------------------------------------------
	 * Primary Palette
	 * ----------------------------------------------------------------------------- */

	/* Primary Palette */
	--sq1-color-java: #1ca8c7;
	--sq1-color-pickled-bluewood: #2b474f;
	--sq1-color-matisse: #1a7f9d;
	--sq1-color-gossamer: #078e87;
	--sq1-color-mandy: #e4554a;
	--sq1-color-casablanca: #f4af49;
	--sq1-color-saffron-mango: #f8ce4d;

	/* Global Colorf Vars */
	--color-primary: var(--sq1-color-java);
	--color-secondary: var(--sq1-color-pickled-bluewood);
	--color-hover: var(--sq1-color-matisse);

	/* -----------------------------------------------------------------------------
	 * Whites
	 * ----------------------------------------------------------------------------- */

	--color-white: #fff;
	--color-off-white: #fafbfd;

	/* -----------------------------------------------------------------------------
	 * Only Shades of Grey
	 * ----------------------------------------------------------------------------- */

	--sq1-color-mine-shaft: #222;
	--sq1-color-outer-space: #323738;
	--sq1-color-nevada: #606a6b;
	--sq1-color-submarine: #b9c2c3;
	--sq1-color-desert-storm: #f2f1f0;
	--sq1-color-white: #fff;

	/* -----------------------------------------------------------------------------
	 * Blacks
	 * ----------------------------------------------------------------------------- */

	--color-black: #000;
	--color-black-a-30: rgba(0, 0, 0, 0.3);
	--color-black-a-60: rgba(0, 0, 0, 0.6);

	/* -----------------------------------------------------------------------------
	 * Backgrounds
	 * ----------------------------------------------------------------------------- */

	--color-bgd: var(--color-white);
	--color-bgd-content: var(--color-white);
	--color-bgd-login: #f1f1f1;

	/* -----------------------------------------------------------------------------
	 * Links
	 * ----------------------------------------------------------------------------- */

	--color-link: var(--color-primary);
	--color-link-hover: var(--color-hover);

	/* -----------------------------------------------------------------------------
	 * Text
	 * ----------------------------------------------------------------------------- */

	--color-text: var(--sq1-color-desert-storm);

	/* -----------------------------------------------------------------------------
	 * Borders
	 * ----------------------------------------------------------------------------- */

	--color-border: var(--sq1-color-submarine);
	--color-table-border: #e0e1e4;
}
