.wp-embed {
	-webkit-column-break-inside: avoid;
	break-inside: avoid-column;

	.t-content & {
		margin: var(--spacer-lg) auto;
		max-width: none;
	}
}

.wp-embed-wrap {
	position: relative;
	aspect-ratio: '16:9';
	overflow: hidden;

	iframe,
	object,
	embed {
		width: 100%;
		height: 100%;
	}
}
