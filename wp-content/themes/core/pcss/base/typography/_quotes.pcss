
/* -----------------------------------------------------------------------------
 * Global Quotes & Blockquotes
 * ----------------------------------------------------------------------------- */

.blockquote,
blockquote,
.quote,
q {

	.t-content & {
		margin: var(--spacer-1) auto;

		@media (--viewport-medium) {
			margin-top: var(--spacer-2);
		}
	}
}

/* -----------------------------------------------------------------------------
 * Quotes
 * ----------------------------------------------------------------------------- */

/* q */
.quote,
.t-content q {
	@mixin quote-base;
	@mixin quote-text;

	display: block;
}

/* -----------------------------------------------------------------------------
 * Blockquotes
 * ----------------------------------------------------------------------------- */

/* blockquote */
.blockquote,
.t-content blockquote {
	@mixin quote-base;

	p {
		@mixin quote-text;

		margin: 0 auto;
		text-align: center;

		& ~ p {
			margin-top: var(--spacer-2);
		}
	}

	cite {
		color: var(--color-mine-shaft);

		@media (--viewport-full) {
			text-align: center;
		}
	}
}

/* -----------------------------------------------------------------------------
 * Cite
 * ----------------------------------------------------------------------------- */

/* cite */
.cite,
.t-content cite {
	display: block;
	font: normal var(--font-weight-light) var(--font-size-1)/var(--line-height-base) var(--font-family-calibre);
	letter-spacing: 1.75px;
	text-align: center;
	text-transform: uppercase;
	position: relative;
	padding-top: var(--spacer-1);
	margin-top: var(--spacer-2);

	&:before {
		content: '';
		width: 24px;
		height: 1px;
		background-color: var(--color-submarine);
		position: absolute;
		left: 50%;
		margin-left: -20px;
		top: -1px;
	}
}
